# Sortable Storage

Lists all storage buildings in a single sortable panel instead of grouped by building type.

## Features
- Sort by Name, Good, Stock (Fill %), Priority, Construction Priority, and Paused status.
- Single unified list for all storage buildings in the district (or global view).
- Persistent sort settings during the session.
